"use strict";
var GeoLocation = (function () {
    function GeoLocation(longitude, latitude) {
        this.longitude = longitude;
        this.latitude = latitude;
    }
    return GeoLocation;
}());
exports.GeoLocation = GeoLocation;
